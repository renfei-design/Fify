# `@fify/a2ui`

Lean A2UI v1.0 message contracts and a deterministic surface reducer for Fify.

```ts
import { applyA2UIMessage, createA2UISurfaceState } from "@fify/a2ui";
```

Use this package when a host or renderer needs the portable protocol state without the semantic compiler or React integration.
